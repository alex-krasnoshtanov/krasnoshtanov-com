{
  "name": "krasnoshtanov-com",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/check.mjs --data && astro build && node scripts/csp.mjs && node scripts/check.mjs --dist",
    "preview": "npm run build && astro preview",
    "check": "astro check",
    "check:data": "node scripts/check.mjs --data",
    "check:dist": "node scripts/check.mjs --dist",
    "prep": "node scripts/prep.mjs",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3",
    "wrangler": "^4.132.0"
  }
}
