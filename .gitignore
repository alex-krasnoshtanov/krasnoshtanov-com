node_modules/
dist/
.astro/
.wrangler/
.dev.vars
# The seeded design canvas is 2.5 MB of generated editor code. The working
# files under design/parts/ are the source of truth and are tracked.
design/krasnoshtanov-site.html

# Throwaway build used only for the clickable preview artifact.
.preview/

# Playwright MCP writes explicitly-named screenshots to the workspace root.
*.png
!public/*.png

# Agent scratch space: worktrees, local settings, session state. CLAUDE.md is
# here too, by choice — it is working notes for whoever is at the keyboard,
# not documentation the repository owes anyone. README.md is the public one.
.claude/
CLAUDE.md
